# frontend（移动端前端，响应式 Web）

依据：§10.3（MVP=响应式 Web，先不做原生壳/Web Push）、附录 B（5 Tab 页面地图）。

- 技术方向：先静态原型（mock 层），后接真接口；验收沿用 playwright（移动视口断点）。
- 主入口 = "把消息贴进来"（§5.0），不做"语音为主"。

> 尚未开工（等待线 A/C 给出 mock 契约，任务板 T7）。当前先用浏览器直开 HTML 验证布局。
